# HTTP Basic Auth credentials (shared by user and admin pages)
BASIC_AUTH_USER=user
BASIC_AUTH_PASSWORD=changeme

# Secret token required for admin POST actions (QR generation, outlaw cards)
SECURE_TOKEN=my-super-secret-token
